// @flow

/*::
  export type EventMap = {
    mail?: string,
    update?: string,
    expunge?: string,
  }
 */

/*::
  export type Config = {
    user: string,
    password: string,
    host: string,
    port: number | string,
    tls?: boolean,
    // node-imap passes this object straight to tls.connect():
    // https://nodejs.org/api/tls.html#tls_tls_connect_options_callback
    tlsOptions?: Object,
    onNotify: string | EventMap,
    onNotifyPost: string | EventMap,
    boxes: Array<string>,
  }
*/
